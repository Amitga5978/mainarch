[package]
name = "mainarch-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Safe device + topology layer over the AMD kernel ABI."

[dependencies]
anyhow.workspace = true
libc.workspace = true
sha2.workspace = true
mainarch-sys = { path = "../mainarch-sys" }
